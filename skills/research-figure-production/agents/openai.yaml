interface:
  display_name: "Research Figure Production"
  short_description: "Select, produce, and audit research figures"
  default_prompt: "Use $research-figure-production in the mode fixed by current.state to create governed figures from an executed notebook with mandatory SciencePlots styling and complete coverage checks."
policy:
  allow_implicit_invocation: false
