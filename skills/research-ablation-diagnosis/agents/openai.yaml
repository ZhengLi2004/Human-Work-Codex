interface:
  display_name: "Research Ablation and Diagnosis"
  short_description: "Distinguish bounded candidate explanations"
  default_prompt: "Use $research-ablation-diagnosis when task-state current.state is ablation_diagnosis."
policy:
  allow_implicit_invocation: false
